<Window x:Class="SmartSightFrontEnd.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SmartSightFrontEnd"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="825">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock Text="Webcam Display" />

        <Image Grid.Row="1" 
               x:Name="WebcamDisplay"
               Height="400"
               Width="400">
        </Image>

        <TextBlock Grid.Column="1"
                   Text="Detected Marker" />

        <Image Grid.Column="1"
               Grid.Row="1" 
               x:Name="DetectionDisplay"
               Height="400"
               Width="400">
        </Image>
    </Grid>
</Window>
